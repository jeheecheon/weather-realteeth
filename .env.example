# Set to "true" to enable React StrictMode (default: disabled when unset).
# Kept off by default because some libraries misbehave under StrictMode's
# double-invocation in dev, e.g. react-use's useGeolocation getting stuck loading.
VITE_STRICT_MODE=true
